# Only pass: cold load is empty paper even with an old store.
Keep paper desk. No tour. No extra chrome. No Solara. No CloudAgent. No git init.
Do not restore the example as default. Ever.
1. BLOCKING: first paint ignores leftover SAMPLE and any example session in localStorage. Bump the persist key. Restore only a draft the user pasted this version. Hard-refresh with an old store must be empty paper: paste a draft, then Run pass. About sits on that empty paper, not after a sample essay.
2. Start over on the paper: one desk action that clears the store and returns to empty paper. Obvious at 375. Not a settings page.
3. Export notice appears next to the tap (footer if they tapped footer), not only at the top of the paper.
4. Two-word gloss on the paper, not a glossary: Type is cut/compressed/rewritten. Track is Voice/Skeptic/Cut/HN. Flow is beats. Take/Leave keeps or drops a mark. Start over is how you go home.
When done print SUCCESS and list changed files.
