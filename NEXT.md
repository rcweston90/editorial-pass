# Next pass. Keep empty-paper cold load. Do not restore the example as default.
Keep SAMPLE, Load example confirm when paper is not empty, Edited empty after load.
Keep paper desk. No extra chrome. No Solara. No CloudAgent. No git init.

1. HEADLINE: headingless flow. Four beats must be four arrivals with distinct labels, not all Opening, not two bars. Arrive from blank-line groups, not a heading parse. Headed essays still use ##.
2. Stats must update after Take. A rewrite taken cannot leave the old in/out percent.
3. Paste and drop land on the blotter paper. Sidebar may keep the optional compare box only.
4. Mark jump labels cannot all be Opening on a headingless draft.
When done print SUCCESS and list changed files.
