# Follow-up after current CLI exits. Do not run while another claude is live.
Keep paper desk. No extra chrome.

HEADLINE do not reorder:
1. Edited box starts empty. Example edited must not pre-fill. Run pass uses original only. Empty edited is never a compare. Pasted original plus Run pass must emit marks.
2. Reload must keep the pasted draft. Cold load may show the example. After paste, persist original and empty edited.
3. Empty state: second box empty. Load example is explicit. First Run pass is not an accidental compare.
4. Export downloads a real md file.
Then split and unified on paper. Flow beats without headings. j goes to next mark not page bottom. Compare must not mark every delta Rewritten.
When done print SUCCESS and list changed files.
Load example must confirm before overwriting. Local pass must emit marks even when the notice says no model key. Do not ask Charlie for a key.
Cold load: empty Original and blank Edited. Load example is a desk action, confirm copy: this replaces what is on the paper. Not a modal. After load, Edited stays empty. Mark the paper SAMPLE. Demo stays but is not the default.

Local pass MUST emit marks even when notice is "Local pass — no model key on this deploy." Do not wait for Vercel ANTHROPIC_API_KEY. Flow beats without ## headings. j jumps to next mark, not page bottom.
