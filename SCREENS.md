One delta then stop. Recut the live blotter into four real screens. Do not deploy.
Charlie GO. Spec is NOT the tighter recut (no Compare/Sections).
Four screens in order. Regular refresh must show screens, not one long blotter:
1. Draft: empty paper. Paste a draft, then Run pass. Cold load stays empty. Never restore SAMPLE on first paint.
2. Marks: after a pass. Decisions are Keep this / Keep mine (not Take / Leave).
3. Original vs this pass: split. Original is the spine. This pass is Edited.
4. Beats: sections as beats. Must not collapse to one Opening. No Flow chrome.
Keep this = keep this delta on the working copy. Keep mine = keep the original for this mark.
Desk example/load (not cold load) uses /workspace/editorial-pass/comparison/original.md and pass.md.
Empty paper stays the default.
Keep vercel.json no-store and Clear-Site-Data. Mini 375. No About, no glossary, no tour.
No git init. No Solara. No CloudAgent. Do not deploy. Local pass engine stays.
When done print SUCCESS and list changed files.
